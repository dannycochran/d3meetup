Testing the Performance of WebGL vs. SVG
=====================

1. Install node v0.10.22 or greater from http://nodejs.org and run:

        npm install
        node server.js

2. Go to localhost:3000/webgl, localhost:3000/sigma, or localhost:3000/svg